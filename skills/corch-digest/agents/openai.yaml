display_name: Corch Digest
short_description: Read, digest, rewrite articles from URLs, and publish to WordPress voice CPT (他山之石)
default_prompt: Read the article at the given URL, digest and rewrite the content, download images, output inline-styled HTML, and optionally publish to WordPress via /clab/v1/publish-voice.
